const Teoria = () => {
    return (
        <section>
            <h2>Componentes de React</h2>
            <p>Los componentes son la base de React. Un componente es una función o clase que devuelve un fragmento de código HTML (JSX) que se renderiza en la interfaz de usuario. <span className="fw-bold reasaltar-parrafo"> Es una pieza reutilizable </span> de la interfaz de usuario.</p>    
        </section>
    );
};

export default Teoria;